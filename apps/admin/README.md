# Admin Dashboard

Separate React/Vite admin application. It stores only the admin token and calls `/api/admin/*` routes.

Required environment variables:

```text
VITE_API_BASE_URL=https://your-api.vercel.app
VITE_CURRENCY=PKR
```

Default seed login is documented at the repository root in `ADMIN_CREDENTIALS.txt`.
